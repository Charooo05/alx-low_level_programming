#include "main.h"
/**
 * print_diagonal - draws a diagonal line on the terminal
 *
 * @n: desired number of diagonal lines
 *
 */

void print_diagonal(int n)
{
	int a

	for (a = 0; a <= n; a++)
	{
		if (n = 0)
			_putchar('\n');

		for (
				_putchar('\\');
				_putchar('\n');


	}
